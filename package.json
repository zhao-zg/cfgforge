{
  "name": "cfgforge-monorepo",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "pnpm -r run build",
    "lint": "eslint packages/ --ext .ts",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
